# NEXT JS TAILWIND APP INITIALIZATIONS

```bash
npx create-next-app@latest . --typescript --tailwind --app --import-alias "@/*" 
npm install framer-motion lucide-react zustand clsx tailwind-merge 
npm install @tailwindcss/typography 
```
